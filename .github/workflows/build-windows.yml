name: Build for Windows

on:
  push:
    branches: '**'
  pull_request:
    branches: '**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
        fetch-depth: 0

    - name: Setup Ninja
      uses: seanmiddleditch/gha-setup-ninja@v5

    - name: Configure and Build
      shell: cmd
      run: |
        :: Inicializar el entorno de Visual Studio
        call "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\VC\Auxiliary\Build\vcvars64.bat" || ^
        call "C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Auxiliary\Build\vcvars64.bat"
        
        :: 1. Excluimos la carpeta de tests explícitamente para evitar que se procese el CMakeLists.txt que busca CUnit
        :: 2. Usamos -DBUILD_TESTING=OFF por precaución
        cmake -S examples/client/udp -B build-client-udp -GNinja -DBUILD_TESTING=OFF
        
        :: Si el paso anterior sigue fallando por la estructura del proyecto, 
        :: podemos forzar la omisión de la carpeta de tests borrándola antes de configurar:
        rd /s /q tests
        
        cmake -S examples/client/udp -B build-client-udp -GNinja -DBUILD_TESTING=OFF
        cmake --build build-client-udp

    - name: Upload lwm2mclient binary
      uses: actions/upload-artifact@v4
      with:
        name: lwm2mclient-windows
        path: build-client-udp/lwm2mclient.exe
        if-no-files-found: error
